# ===============
#  .curlrc
# ===============

--progress-bar
--show-error

--connect-timeout 5

--proto-default https
--proto-redir -all,http,https

#--write-out "%{filename_effective} (%{content_type}) | %{time_total} sec, %{size_download} byte\n%{url_effective}\n"


# HTTP
--location
--max-redirs 4

--remote-header-name
--remote-time

--referer ";auto"
